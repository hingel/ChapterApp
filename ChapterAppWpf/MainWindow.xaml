<Window x:Class="ChapterAppWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChapterAppWpf"
        xmlns:viewModels="clr-namespace:ChapterAppWpf.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        xmlns:viewModel="clr-namespace:ChapterAppWpf.ViewModels"
        xmlns:views="clr-namespace:ChapterAppWpf.Views">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewModels:StartViewModel}">
                <views:StartView/>
            </DataTemplate>
        </Grid.Resources>
        <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
    </Grid>
</Window>
